interface:
  display_name: "KeePassBrowserBridge"
  short_description: "Repo-specific patterns and workflows for KeePassBrowserBridge"
  default_prompt: "Use the KeePassBrowserBridge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true